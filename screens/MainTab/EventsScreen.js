import React from 'react';
import {
  Text,
} from 'react-native';

export default class EventsScreen extends React.Component {
  static navigationOptions = {
    title: "Soon events"
  };

  render() {
    return (
      <Text>Events should be here!</Text>
    );
  }
}